playback_slower = input()
playback_slower = playback_slower.replace(" ", "...") # we replace in string playback slower any
# " " symbol to "..." with function replace and return this value to our variable
print(playback_slower)